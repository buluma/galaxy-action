---
name: Yaml Lint
on:
  push:
  workflow_dispatch:
jobs:
  yamllint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: yamllint
        uses: ibiqlik/action-yamllint@v3
